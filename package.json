{
  "name": "lolyou-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync-content": "node scripts/sync-content.mjs",
    "dev": "npm run sync-content && vite",
    "build": "npm run sync-content && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
